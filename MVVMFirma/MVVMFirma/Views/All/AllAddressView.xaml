<local:AllViewBase x:Class="MVVMFirma.Views.AllAddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding Path=SelectedAdress}" Margin="0,0,0,-354">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id Address" Binding="{Binding IdAdress}"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}"/>
                <DataGridTextColumn Header="Street" Binding="{Binding Street}"/>
                <DataGridTextColumn Header="Nr Home" Binding="{Binding NrHome}"/>
                <DataGridTextColumn Header="Nr Local" Binding="{Binding NrLocal}"/>
                <DataGridTextColumn Header="Zip Code" Binding="{Binding ZipCode}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
