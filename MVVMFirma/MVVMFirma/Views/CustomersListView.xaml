<local:AllViewBase x:Class="MVVMFirma.Views.CustomersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" Margin="0,0,0,-1506">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" />
                <DataGridTextColumn Header="Name" />
                <DataGridTextColumn Header="Phone" />
                <DataGridTextColumn Header="Email" />
                <DataGridTextColumn Header="Notes" />
                <DataGridTextColumn Header="Created At" />
                <DataGridTextColumn Header="Updated At" />
                <DataGridTextColumn Header="Is Primary" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
