<local:AllViewBase x:Class="MVVMFirma.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" Margin="0,0,0,-354">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdLog}"/>
                <DataGridTextColumn Header="Action Type" Binding="{Binding ActionType}"/>
                <DataGridTextColumn Header="Invoice Number" Binding="{Binding InvoiceNumber}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
