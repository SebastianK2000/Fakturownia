<UserControl x:Class="MVVMFirma.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdSettings}"/>
                <DataGridTextColumn Header="Language" Binding="{Binding Language}"/>
                <DataGridTextColumn Header="Login" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Password" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Payment Deadline Trigger" Binding="{Binding PaymentDeadlineTrigger}"/>
                <DataGridTextColumn Header="New Inovice Trigger" Binding="{Binding NewInoviceTrigger}"/>
                <DataGridTextColumn Header="Pyment Not Made" Binding="{Binding PymentNotMade}"/>
                <DataGridTextColumn Header="Id Customer" Binding="{Binding IdCustomer}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
