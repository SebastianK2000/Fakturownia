<UserControl x:Class="MVVMFirma.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" />
                <DataGridTextColumn Header="Language" />
                <DataGridTextColumn Header="Login" />
                <DataGridTextColumn Header="Password" />
                <DataGridTextColumn Header="Payment Deadline Trigger" />
                <DataGridTextColumn Header="New Inovice Trigger" />
                <DataGridTextColumn Header="Pyment Not Made" />
                <DataGridTextColumn Header="Id Customer" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
